// theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ededed",
     },
    secondary: {
      main: "#457B7B",
    },
  },
 
});

export default theme;
